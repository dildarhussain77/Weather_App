/// Relative paths for the OpenWeather Dio base URL (`AppHttpClient` in `data/`).
abstract final class ApiEndpoints {
  static const String weather = 'weather';
  static const String find = 'find';
}
